{
  "private": true,
  "description": "Lint tooling for quarkus-pha (installed via Podman, never on host)",
  "devDependencies": {
    "eslint": "^9.20.0",
    "stylelint": "^16.14.1",
    "stylelint-config-standard": "^37.0.0",
    "htmlhint": "^1.5.0"
  }
}
