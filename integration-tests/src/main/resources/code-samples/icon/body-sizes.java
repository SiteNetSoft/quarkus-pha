import org.sitenetsoft.quarkus.pha.model.*;

Icon iconBodySm = Icon.of("fa:star").size("body-sm").build();

Icon iconBodyDefault = Icon.of("fa:star").size("body-default").build();

Icon iconBodyLg = Icon.of("fa:star").size("body-lg").build();

// Template side, with the data in scope:
// {#include components/icon iconModel=icon... /} (one include per icon)
